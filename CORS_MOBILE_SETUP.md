# راهنمای اتصال موبایل به Backend

## تنظیمات انجام شده:
✅ IP address سرور به صورت خودکار به CORS_ALLOWED_ORIGINS اضافه می‌شود
✅ IP فعلی: 192.168.247.68

## نحوه استفاده از موبایل:

### 1. اطمینان از اتصال به همان شبکه WiFi
- موبایل و کامپیوتر باید به یک WiFi متصل باشند

### 2. دسترسی به Frontend از موبایل:
- در مرورگر موبایل، به این آدرس بروید:
  ```
  http://192.168.247.68:3000
  ```

### 3. اگر می‌خواهید همه origins را قبول کنید (فقط برای development):
در فایل `.env` در پوشه backend اضافه کنید:
```
CORS_ALLOW_ALL_ORIGINS=True
```

⚠️ **هشدار:** این تنظیم فقط برای development است و هرگز در production استفاده نکنید!

## بررسی:
- Backend باید روی `http://192.168.247.68:8000` در دسترس باشد
- Frontend باید روی `http://192.168.247.68:3000` در دسترس باشد
- هر دو باید در همان شبکه محلی باشند

## اگر IP تغییر کرد:
- Backend را restart کنید تا IP جدید به صورت خودکار اضافه شود
- یا در `.env` تنظیم کنید: `CORS_ALLOW_ALL_ORIGINS=True`
